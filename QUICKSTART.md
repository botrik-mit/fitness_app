# 🚀 Быстрый старт

## 1️⃣ Настройка Google Apps Script (5 минут)

1. Откройте https://script.google.com
2. Создайте новый проект
3. Скопируйте содержимое `Code.gs` в редактор
4. Создайте новую Google Sheets таблицу: https://sheets.google.com
5. Скопируйте ID таблицы из URL и замените в `Code.gs`:
   ```javascript
   const SPREADSHEET_ID = 'ВАШ_ID_ТАБЛИЦЫ';
   ```
6. Нажмите `Deploy` → `New deployment` → `Web app`
7. Настройки:
   - Execute as: `Me`
   - Who has access: `Anyone`
8. Скопируйте URL веб-приложения

## 2️⃣ Настройка приложения (1 минута)

1. Откройте `app.js`
2. Найдите строку:
   ```javascript
   const GOOGLE_APPS_SCRIPT_URL = '...';
   ```
3. Замените на ваш URL из шага 1

## 3️⃣ Подготовка к GitHub (2 минуты)

1. Переименуйте `index-github.html` в `index.html`:
   ```bash
   mv index-github.html index.html
   ```

2. Создайте папку `icon/` и добавьте иконки (или используйте placeholder)

## 4️⃣ Деплой на GitHub (3 минуты)

1. Создайте репозиторий на GitHub
2. Загрузите файлы:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/USERNAME/REPO.git
   git push -u origin main
   ```
3. В настройках репозитория: `Settings` → `Pages` → выберите `main` branch
4. Готово! Приложение доступно по адресу: `https://USERNAME.github.io/REPO/`

## ✅ Итого: 11 минут до запуска!

## 📱 Как установить на телефон

**iPhone:**
1. Откройте в Safari
2. Нажмите кнопку "Поделиться" 
3. "На экран «Домой»"

**Android:**
1. Откройте в Chrome
2. Меню → "Установить приложение"

---

## ❓ Нужна помощь?

Откройте полную инструкцию: [README.md](README.md)
