# 🏋️ План Роста и Тренировок

Веб-приложение для планирования тренировок и питания на 12 недель с синхронизацией данных в Google Sheets.

## 📋 Возможности

- ✅ Планирование тренировок по дням недели
- ✅ Отслеживание весов и RPE (оценка нагрузки)
- ✅ Ведение дневника тренировок
- ✅ Планирование питания и добавок
- ✅ Статистика по неделям и общая
- ✅ Синхронизация данных с Google Sheets
- ✅ Работает как PWA (Progressive Web App)
- ✅ Темная/светлая тема
- ✅ Адаптивный дизайн (мобильные, планшеты, десктоп)

## 🚀 Деплой на GitHub Pages

### Шаг 1: Подготовка Google Apps Script

1. Откройте ваш проект в [Google Apps Script](https://script.google.com)

2. Скопируйте содержимое файла `Code.gs` в редактор Apps Script

3. **Создайте Google Sheets таблицу** для хранения данных:
   - Создайте новую таблицу в [Google Sheets](https://sheets.google.com)
   - Скопируйте ID таблицы из URL: `https://docs.google.com/spreadsheets/d/ВАШ_ID/edit`
   - Замените `SPREADSHEET_ID` в `Code.gs` на ваш ID

4. **Деплой Apps Script как веб-приложение**:
   - Нажмите `Deploy` → `New deployment`
   - Выберите тип: `Web app`
   - Настройки:
     - **Description**: `Fitness App API`
     - **Execute as**: `Me (ваш email)`
     - **Who has access**: `Anyone` (если хотите публичный доступ) или `Only myself`
   - Нажмите `Deploy`
   - **Скопируйте URL** веб-приложения (он будет выглядеть как `https://script.google.com/macros/s/...`)

### Шаг 2: Настройка проекта

1. Откройте файл `app.js`

2. Найдите строку:
```javascript
const GOOGLE_APPS_SCRIPT_URL = 'https://script.google.com/macros/s/...';
```

3. Замените URL на ваш URL из Шага 1

### Шаг 3: Подготовка иконок

Создайте папку `icon/` и добавьте иконки приложения:

```
icon/
  ├── favicon-16x16.png
  ├── favicon-32x32.png
  ├── apple-touch-icon.png (180x180)
  ├── android-chrome-192x192.png
  └── android-chrome-512x512.png
```

Или используйте сервисы:
- [Favicon.io](https://favicon.io/)
- [RealFaviconGenerator](https://realfavicongenerator.net/)

### Шаг 4: Загрузка на GitHub

1. Создайте новый репозиторий на [GitHub](https://github.com)

2. Загрузите файлы:
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_REPO.git
git push -u origin main
```

### Шаг 5: Включение GitHub Pages

1. Откройте настройки репозитория: `Settings` → `Pages`

2. Настройте:
   - **Source**: `Deploy from a branch`
   - **Branch**: `main`
   - **Folder**: `/ (root)`

3. Нажмите `Save`

4. Переименуйте `index-github.html` в `index.html` перед загрузкой:
```bash
mv index-github.html index.html
git add index.html
git commit -m "Rename to index.html"
git push
```

5. Через 1-2 минуты ваше приложение будет доступно по адресу:
```
https://ВАШ_USERNAME.github.io/ВАШ_REPO/
```

## 📱 Установка как PWA

### На Android (Chrome)

1. Откройте приложение в браузере Chrome
2. Нажмите на меню (три точки) → "Установить приложение"
3. Подтвердите установку

### На iOS (Safari)

1. Откройте приложение в Safari
2. Нажмите кнопку "Поделиться" (квадрат со стрелкой)
3. Выберите "На экран «Домой»"
4. Подтвердите добавление

### На Desktop (Chrome/Edge)

1. Откройте приложение в браузере
2. Нажмите на иконку установки в адресной строке (справа)
3. Нажмите "Установить"

## 🔧 Структура проекта

```
fitnes_prog/
├── index-github.html    # HTML для GitHub Pages
├── app.js              # JavaScript логика приложения
├── manifest.json       # PWA manifest
├── sw.js              # Service Worker
├── Code.gs            # Google Apps Script backend
├── icon/              # Иконки приложения
└── README.md          # Эта инструкция
```

## 🔐 Аутентификация

Приложение использует простую аутентификацию по email:

1. При первом запуске введите ваш email
2. Данные будут сохраняться в Google Sheets под вашим email
3. Email сохраняется в `localStorage` браузера

**Важно**: Это базовая аутентификация без проверки. Для продакшена рекомендуется использовать Google OAuth.

## 📊 Google Sheets структура

Таблица имеет 2 колонки:
- **A (email)**: Email пользователя
- **B (data)**: JSON с данными пользователя

Структура данных:
```json
{
  "trainingData": {...},
  "week": 1,
  "weekStats": [...],
  "theme": "light",
  "nutritionText": "...",
  "supplements": {...},
  "tasks": {...},
  "weights": {...},
  "rpe": {...},
  "comments": {...},
  "progress": 0
}
```

## 🐛 Решение проблем

### PWA не устанавливается

- Убедитесь, что сайт доступен по HTTPS (GitHub Pages автоматически использует HTTPS)
- Проверьте, что `manifest.json` и `sw.js` загружаются без ошибок
- Откройте Chrome DevTools → Application → Manifest и проверьте ошибки

### Данные не сохраняются

- Откройте Chrome DevTools → Console и проверьте ошибки
- Убедитесь, что `GOOGLE_APPS_SCRIPT_URL` правильный
- Проверьте настройки деплоя Apps Script (должно быть `Anyone` или `Anyone with the link`)
- Проверьте, что у вас есть права на запись в Google Sheets

### CORS ошибки

Google Apps Script автоматически обрабатывает CORS, но если возникают проблемы:
- Убедитесь, что используете правильный URL веб-приложения (не URL редактора!)
- Проверьте, что деплой актуален (может потребоваться новый деплой после изменений)

## 📝 Лицензия

MIT License - используйте свободно!

## 👤 Автор

Создано для личного использования. Можете форкнуть и адаптировать под себя!

## 🙏 Благодарности

- Google Apps Script за бесплатный backend
- GitHub Pages за бесплатный хостинг
- Всем, кто тренируется и достигает целей! 💪
